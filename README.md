CS 5720
700688700
Nathanael Nading
Assignment 2

# Part 1

> Write a program that takes two strings from the user: first_name, last_name. Pass these variables to
> fullname function that should return the (full name).
> Write function named “string_alternative” that returns every other char in the full_name string.

The script part1.py can be run with the following command:
    python part1.py

# Part 2

> Write a python program to find the wordcount in a file (input.txt) for each line and then print the output.
> Finally store the output in output.txt file.

The script part2.py can be run with the following command:
    python part2.py
When running, it will search for an input.txt file in the same directory as the script.
It will also output a file called output.txt.

# Part 3

> Write a program, which reads heights (inches.) of customers into a list and convert these
> heights to centimeters in a separate list using:
> - Nested Interactive loop.
> - List comprehensions

The script part3.py can be run with the following command:
    python part3.py

https://youtu.be/wo6Aw-0yr_0
